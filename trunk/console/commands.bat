@echo off
cls
echo Los comandos que se pueden usar aqui son:
echo.
echo * memory, especifica memoria RAM (en MB).
echo * iso, escribe "iso" y arrastra el archivo ISO a emular.
echo * hda, escribe si quieres usar el HDA de Linux Launcher o no (hda on/off)
echo * floppy, escribe "floppy" y arrastra el archivo IMG u otro para emular.
echo * fullscreen, escribe si quieres ver la emulacion a pantalla completa
echo   (fullscreen on/off)
echo * kqemu, escribe si quieres usar KQemu (en algunas distros puede que
echo   sea mejor desactivarlo, kqemu on/off)
echo * localtime, escribe si quieres que la emulacion tenga el mismo tiempo
echo   que tu ordenador (localtime on/off)
echo * boot, especifica que unidad arrancara (a = floppy, c = hda, d = cd)
echo * options, especifica las opciones que has configurado.
echo * runlinux, ejecuta las opciones que has configurado.
echo * debugrun, ejecuta las opciones que has configurado mostrando posibles
echo   mensajes de error.
echo.